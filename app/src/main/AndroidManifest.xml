<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".activityToolBar"
            android:exported="false"
            android:parentActivityName=".MainActivity"/>
        <!-- definimos el padre de la actividad, en este caso MainActivity, esto afecta la navegación entre actividades -->
        <activity
            android:name=".gridViewPersonal"
            android:exported="false" />
        <activity
            android:name=".gridView"
            android:exported="false" />
        <activity
            android:name=".Listas"
            android:exported="false" />
        <activity
            android:name=".ListViewPersonal"
            android:exported="false" />
        <activity
            android:name=".ListView"
            android:exported="false" />
        <activity
            android:name=".Clima"
            android:exported="false" />
        <activity
            android:name=".Ciudades"
            android:exported="false" />
        <activity
            android:name=".WebApi"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>